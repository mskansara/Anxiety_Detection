import { Button, Grid } from '@mui/material'
import React from 'react'

function SessionHeader() {
    return (
        <Grid
        >
            <Button variant='contained'>Start a new session</Button>
        </Grid>

    )
}

export default SessionHeader